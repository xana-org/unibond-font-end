import { ethers } from "ethers";
import BigNumber from "bignumber.js";
import {
    WETH_ADDRESS,
} from "../utils/const";
import {
    getBalance,
} from "../contracts/erc20";


const getWETHPrice = async (token, pair, chainId) => {
    
}